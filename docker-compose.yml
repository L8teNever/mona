services:
  mona:
    image: ghcr.io/l8tenever/mona:latest
    ports:
      - "5000:5000"
    volumes:
      - mona-data:/data
    environment:
      - MONA_DATA_DIR=/data
    restart: unless-stopped

volumes:
  mona-data:
