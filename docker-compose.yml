services:
  mona:
    image: ghcr.io/l8tenever/mona:latest
    ports:
      - "5000:5000"
    volumes:
      - mona-data:/data
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - MONA_DATA_DIR=/data
    restart: unless-stopped

volumes:
  mona-data: